experiment:
  name: experiment_5c_local
  output_dir: results/experiment_5c_local
  dataset_root: UCRArchive_2018
  dataset_ext: tsv
  dataset_format: ucr
  seeds: [42]

  datasets:
    - name: SyntheticControl
      num_classes: 6

  chart_types: [line]

model:
  type: single_modal_chart
  chart_model: resnet18
  pretrained: false

training:
  batch_size: 64
  epochs: 30
  learning_rate: 0.0005

image_generation:
  generate_images: true
  overwrite_existing: false

chart:
  color_mode: color
  label_mode: with_label
